import 'dart:ui';

/// Colour theme for the app.
class Colours {
  /// Lighter base colour.
  static const Color lightBase = Color(0xFF716F86);

  /// Normal base colour.
  static const Color base = Color(0xFF3E3C52);

  /// Darker base colour.
  static const Color darkBase = Color(0xFF1C1C2F);

  /// Light text colour.
  static const Color text = Color(0xFFF9F7FF);

  /// Dark text colour.
  static const Color darkText = Color(0xFF9997B0);

  /// Default red colour.
  static const Color red = Color(0xFFCE3133);
}
